import { AlertTriangle, CheckCircle2, Inbox } from 'lucide-react';
import { cn } from '@/lib/utils';

export function EmptyState({ title, description, action, compact = false }: { title: string; description: string; action?: React.ReactNode; compact?: boolean }) { return <div className={cn('rounded-[1.5rem] border border-dashed bg-card text-center', compact ? 'p-6' : 'p-10')}><span className="mx-auto grid size-11 place-items-center rounded-full bg-muted text-secondary"><Inbox size={20}/></span><h2 className="font-display mt-4 text-2xl">{title}</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-muted-foreground">{description}</p>{action && <div className="mt-5">{action}</div>}</div>; }
export function StatusBanner({ tone = 'success', title, children }: { tone?: 'success' | 'warning' | 'danger'; title: string; children: React.ReactNode }) { const Icon=tone==='success'?CheckCircle2:AlertTriangle; return <div role="status" className={cn('rounded-2xl border p-4', tone==='success'&&'border-emerald-200 bg-emerald-50',tone==='warning'&&'border-amber-200 bg-amber-50',tone==='danger'&&'border-red-200 bg-red-50')}><div className="flex gap-3"><Icon size={19} className={cn('mt-0.5 shrink-0',tone==='success'?'text-emerald-700':tone==='warning'?'text-amber-700':'text-red-700')}/><div><p className="font-semibold">{title}</p><div className="mt-1 text-sm leading-6 text-muted-foreground">{children}</div></div></div></div>; }
export function Skeleton({ className = '' }: { className?: string }) { return <div aria-hidden className={cn('animate-pulse rounded-xl bg-muted',className)}/>; }
